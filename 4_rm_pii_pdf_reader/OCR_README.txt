# To use OCR for scanned PDFs, install these dependencies:
pip install pytesseract pillow
# You must also install Tesseract OCR engine:
# Windows: Download the installer from https://github.com/tesseract-ocr/tesseract
# and add the Tesseract install directory to your PATH.
# Example: C:\Program Files\Tesseract-OCR
# Linux: sudo apt-get install tesseract-ocr
# Mac: brew install tesseract
